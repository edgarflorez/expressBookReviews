Registe Login
curl localhost:5000/register -H "Content-Type: application/json" -d '{ "username": "Edgar", "password": "12345" }' && curl -c cookies.txt -X POST localhost:5000/customer/login -H "Content-Type: application/json" -d '{ "username": "Edgar", "password": "12345" }'

Add review and get review
curl -b cookies.txt -X PUT "http://localhost:5000/customer/auth/review/1?review=GreatBook" && curl localhost:5000/review/1 && curl -b cookies.txt -X DELETE "http://localhost:5000/customer/auth/review/1" && curl localhost:5000/review/1


Feedback 

The submitted URL contains the repository named 'expressBookReviews' and directly redirects to the 'general.js' file. The file includes a working code implementation that retrieves book details based on the author using async/await. However, the implementation does not correctly handle the author parameter from the request, as it does not filter the book data accurately. The response handling for cases where no book is found is also not implemented correctly, as it attempts to resolve within a loop without properly managing the response for cases where no matches are found. To improve, consider implementing a more robust filtering mechanism for the author parameter and ensure that the HTTP response handling includes success, not found, and error cases.